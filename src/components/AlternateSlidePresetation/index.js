import React from 'react';
import PropTypes from 'prop-types';

const AlternateSlidePresentation = props => {
  return (
    <div className="">

    </div>
  );
};

AlternateSlidePresentation.propTypes = {

};

export default AlternateSlidePresentation;

import TopNav from "./index";

export default {
  "title": "Layout/TopNav",
  component: TopNav
}

const Template = args => <TopNav {...args} />;

export const Default = Template.bind({});
Default.args = {

}

import React from 'react';
import PropTypes from 'prop-types';

const TopNav = props => {
  return (
    <nav>
      <div>
        <span>Hello</span>
      </div>
    </nav>
  );
};

TopNav.propTypes = {

};

export default TopNav;

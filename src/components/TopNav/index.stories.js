import TopNav from "./index";

export default {
  "title": "TopNav",
  component: TopNav
}

const Template = args => <TopNav {...args} />;

export const Default = Template.bind({});
Default.args = {

}
